<!DOCTYPE html>
<html>
<head>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap-theme.min.css">
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
	<title>Apple Refurbish v0.1</title>
</head>
<body>
	<style type="text/css">
	</style>
	<table class="table table-hover table-condensed">
		<thead>
			<tr>
				<th>#</th>
				<th>Фото</th>
				<th>Лот</th>
				<th>Описание</th>
				<th>Дата производства</th>
				<th>Цена</th>
				<th>Скидка</th>
			</tr>
		</thead>
		<tbody>
			{{#each items}}
				<tr class="
					{{#if __withDiff}}
						warning
					{{/if}}
				">
					<td>{{@index}}</td>
					<td><img src="{{productImageUrl}}"></td>
					<td><a target="blank" href="{{url}}">{{uid}}</a></td>
					<td>
						<h4>{{name}}</h4>
						{{breaklines description}}
					</td>
					<td>{{moment origReleased format="MMM YYYY"}}</td>
					{{!-- <td>{{price}}$</td> --}}
					<td>{{math price '*' ../USD 2}}&#8381;</td>
					{{!-- <td>{{saveVal}}$</td> --}}
					<td>
						{{math saveVal '*' ../USD 2}}&#8381; </br>
						{{savePer}}%
					</td>
				</tr>
			{{/each}}
		</tbody>
	</table>
</body>
</html>